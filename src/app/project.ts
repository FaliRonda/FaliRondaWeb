import { SafeResourceUrl} from '@angular/platform-browser';

export class Project {
  id: number;
  name: string;
  imgUrl: string;
  gameUrl: string;
  jobs: string;
  information: string;
  description: string;
  codeLink: string;
}
